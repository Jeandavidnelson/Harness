# BMAD adapter

This adapter adds no policy logic. A BMAD development workflow should call the universal JSON interface and treat the deterministic harness as the validation authority.

Use [workflow-snippet.md](workflow-snippet.md) as a reusable step sequence. The engine remains usable without BMAD and BMAD never edits `rules.yaml` automatically.

