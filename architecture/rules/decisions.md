# Architecture rule decisions

Only rules explicitly reviewed by a human may move from `candidates.yaml` to `rules.yaml` with `status: validated` and `provenance: USER_CONFIRMED`.

| Date | Rule | Decision | Reviewer | Rationale |
|---|---|---|---|---|
| — | — | — | — | No V2 candidate promoted yet. |
